#!/bin/sh
mount "/dev/disk/by-label/XenServer\x20Tools" /mnt
cd /mnt/Linux
case $(uname -m) in
*64) dpkg -i *x86_64.deb ;;
*) dpkg -i *i386.deb ;;
esac

mkdir /root/.ssh/
echo "{{ pubkey }}" >> /root/.ssh/authorized_keys
chmod  700 /root/.ssh
chmod -R 600 /root/.ssh/*

