# Kickstart file automatically generated by anaconda.

#version=DEVEL
install
url --url=http://mirror.corbina.net/centos/6/os/x86_64/
lang en_US.UTF-8
keyboard us
network --onboot yes --device eth0 --bootproto static --ip 10.10.10.21 --netmask 255.255.255.0 --gateway 10.10.10.254 --noipv6 --nameserver 10.10.12.7
rootpw  password
firewall --service=ssh
authconfig --enableshadow --passalgo=sha512
selinux --enforcing
timezone --utc Europe/Moscow
bootloader --location=mbr --driveorder=xvda --append="crashkernel=auto console=hvc0 rhgb quiet"
# The following is the partition information you requested
# Note that any partitions you deleted are not expressed
# here so unless you clear all partitions first, this is
# not guaranteed to work
#clearpart --linux --drives=xvda
#volgroup VolGroup --pesize=4096 pv.202002
#logvol swap --name=lv_swap --vgname=VolGroup --grow --size=819 --maxsize=819

#part /boot --fstype=ext4 --size=500
#part pv.202002 --grow --size=1

repo --name="CentOS"  --baseurl=http://mirror.corbina.net/centos/6/os/x86_64/ --cost=100

%packages --nobase
@core
%end