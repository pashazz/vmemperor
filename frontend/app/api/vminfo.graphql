query VMInfo ($uuid: ID!) {
  vm (uuid: $uuid) {
    uuid
    nameLabel
    nameDescription
    interfaces {
      network {
        uuid
        nameLabel
      }
      ip
      ipv6
      id
    }
    powerState
    osVersion{
      name
    }
    startTime
    domainType
  }
}
