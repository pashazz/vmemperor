/*
 * VmSettings Messages
 *
 * This contains all the text for the VmSettings component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  header: {
    id: 'app.containers.VmSettings.header',
    defaultMessage: 'This is VmSettings container !',
  },
});
